//Scrivi un programma che legge in input il valore di due lati di un quadrilatero, individua se si tratta di un quadrato o di un rettangolo e ne calcola il perimetro e l’area//
#include <stdio.h>
    int main(){
        int n1;
        int n2;
        int perimetro;
        int area;
        int quadrilater;
        printf("Scrivi un valore")
        scanf("%d", &n1)
        printf("Scrivi un valore")
            scanf("%d", &n2)
        if (n1==n2){
            printf("Il quadrilatero è un quadrato")
            quadrilatero=quadrato
        else
            if (n1!=n2)
                printf("Il quadrilatero è un rettangolo")
                quadrilatero=rettangolo;
        }
        if (quadrilatero==quadrato){
            perimetro=(n1*4)
            area=(n1*n1)
            printf("L'area del quadrato è:", area)
            printf("Il perimetro del quadrato:", perimetro)
        else
            if (quadrilatero==rettangolo)
            perimetro=(n2*2)+(n1*2);
            area=n1*n2;
            printf("L'area del rettangolo è:", area)
            printf("Il perimetro è:", perimetro)
        }
        return 0;
}     

